{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "Real value lives in dotnet user-secrets (dev) or env vars (prod). NEVER commit a connection string here.",
    "Postgres": ""
  }
}
